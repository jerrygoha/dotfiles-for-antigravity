# /execute-plan 가이드

> 수립된 계획을 단계별로 실행하며 검증하는 워크플로우

---

## 📌 언제 사용하나요?

- `/write-plan`으로 계획 수립 완료 후
- 사용자가 계획을 승인한 후

---

## 📤 결과물

### Phase 완료 시

```markdown
## Phase 1 Complete ✅

**Done:** [완료된 작업]
**Verified:** Build ✅, Tests ✅
**Next:** Phase 2 - [설명]
Continue? [Y/N]
```

### 전체 완료 시

```markdown
## Execution Complete ✅

### Completed Phases
- [x] Phase 1, 2, ...

### Rollback Command
git revert [commit-range]
```

---

## 💡 사용법

```
/execute-plan
```

계획서가 로드되고 Phase별로 실행 → 검증 → 승인 반복

---

## ⚠️ 이럴 땐 다른 워크플로우!

| 상황 | 대신 사용 |
|------|----------|
| 계획이 없음 | `/write-plan` 먼저 |
| 빌드 에러 | `/fix-ci` |
| 테스트 실패 | `/debug` |

---

## 🔗 권장 흐름

```
/write-plan → /execute-plan → (Phase별 반복) → /code-review → /create-pr
```

**장시간 작업 시:**
```
Phase 1,2 완료 → /handoff → (새 세션) → /pickup → Phase 3 계속
```
