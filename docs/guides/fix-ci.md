# /fix-ci 가이드

> CI/CD 파이프라인 실패를 진단하고 해결하는 워크플로우

---

## 📌 언제 사용하나요?

| 상황 | 예시 |
|------|------|
| CI 빌드 실패 | "GitHub Actions 실패" |
| 로컬은 되는데 CI는 안 됨 | "내 컴퓨터에선 돼요" |
| 테스트 Flaky | "가끔 실패해요" |
| 환경 차이 의심 | "CI에서만 에러" |

---

## 📤 실패 카테고리

| 카테고리 | 일반 원인 | 빠른 확인 |
|----------|---------|----------|
| Build | 의존성 누락, 문법 | `npm ci && npm run build` |
| Lint | 스타일 위반 | `npm run lint` |
| Test | Assertion 실패 | `npm test` |
| Deploy | 설정, 시크릿 | 환경변수 확인 |
| Timeout | 느린 테스트 | 최적화 또는 limit 조정 |

---

## ⚠️ 핵심: 로컬 재현

> 로컬에서 재현되지 않으면 환경 차이 문제일 가능성 높음!

```bash
# CI 환경과 동일하게
rm -rf node_modules
npm ci
npm run lint
npm test
npm run build
```

---

## 📤 일반적인 해결책

| 문제 | 해결 |
|------|------|
| Node 버전 불일치 | `.nvmrc`와 CI 설정 일치 |
| 환경변수 누락 | CI secrets에 추가 |
| Lock file 불일치 | `package-lock.json` 재생성 |
| Flaky 테스트 | 재시도 로직, race condition 수정 |

---

## 🔗 권장 흐름

```
/fix-ci → /testing (테스트 보강) → /code-review → /create-pr
```
